// Redux store configuration
// This would combine all reducers and configure the store
// For now, just exporting the slice states

export * from "./slices/authSlice";
export * from "./slices/userSlice";
